diff --git a/front-end/src/pages/BrowserCataloguePage.jsx b/front-end/src/pages/BrowserCataloguePage.jsx
index e57b29456..e9556ae8f 100644
--- a/front-end/src/pages/BrowserCataloguePage.jsx
+++ b/front-end/src/pages/BrowserCataloguePage.jsx
@@ -9,8 +9,9 @@ export default function BrowserCataloguePage({ onNavigate, setSelectedFacility }
 
   const facilities = data?.facilities ?? [];
 
-  const handleFacilityClick = (facilityName) => {
-    setSelectedFacility(facilityName);
+  const handleFacilityClick = (facility) => {
+    // Pass the whole facility object for downstream pages needing _id and name
+    setSelectedFacility(facility);
     onNavigate('facilityItems');
   };
 
@@ -39,14 +40,19 @@ export default function BrowserCataloguePage({ onNavigate, setSelectedFacility }
           </h2>
 
           {error && (
-            <div className="mb-4 p-4 bg-red-50 border border-red-200 text-sm text-red-700 rounded-lg">
-              Unable to load facilities.
-              <button
-                onClick={refetch}
-                className="ml-1 underline hover:text-red-800"
-              >
-                Retry
-              </button>
+            <div className="mb-4 p-4 bg-purple-50 border border-purple-200 text-sm text-purple-800 rounded-lg flex items-start gap-3">
+              <svg className="w-5 h-5 text-purple-500 flex-shrink-0 mt-0.5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
+                <path strokeLinecap="round" strokeLinejoin="round" strokeWidth={2} d="M12 8v4m0 4h.01M21 12a9 9 0 11-18 0 9 9 0 0118 0z" />
+              </svg>
+              <span>
+                Unable to load facilities.
+                <button
+                  onClick={refetch}
+                  className="ml-1 underline hover:text-purple-900"
+                >
+                  Retry
+                </button>
+              </span>
             </div>
           )}
 
@@ -56,8 +62,8 @@ export default function BrowserCataloguePage({ onNavigate, setSelectedFacility }
             ) : (
               facilities.map((facility) => (
                 <button
-                  key={facility.id ?? facility.name}
-                  onClick={() => handleFacilityClick(facility.name)}
+                  key={facility._id || facility.id || facility.name}
+                  onClick={() => handleFacilityClick(facility)}
                   className="w-full py-4 px-6 bg-white border-2 border-gray-200 rounded-xl text-gray-700 font-medium hover:border-violet-300 hover:bg-violet-50 transition-all text-center"
                 >
                   {facility.name}
