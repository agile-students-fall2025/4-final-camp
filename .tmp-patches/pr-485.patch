diff --git a/front-end/src/pages/ReservationConfirmedPage.jsx b/front-end/src/pages/ReservationConfirmedPage.jsx
index d1fde90ca..4c692503c 100644
--- a/front-end/src/pages/ReservationConfirmedPage.jsx
+++ b/front-end/src/pages/ReservationConfirmedPage.jsx
@@ -1,5 +1,6 @@
-import React from 'react';
+import React, { useState } from 'react';
 import { ChevronLeft } from 'lucide-react';
+import { api } from '../services/api.js';
 
 export default function ReservationConfirmedPage({ onNavigate, reservationData }) {
   if (!reservationData) {
@@ -7,10 +8,22 @@ export default function ReservationConfirmedPage({ onNavigate, reservationData }
     return null;
   }
 
-  const handleCancelReservation = () => {
-    if (confirm('Are you sure you want to cancel this reservation?')) {
+  const [cancelling, setCancelling] = useState(false);
+  const handleCancelReservation = async () => {
+    if (!reservationData?.id) {
+      alert('Missing reservation ID');
+      return;
+    }
+    if (!confirm('Are you sure you want to cancel this reservation?')) return;
+    try {
+      setCancelling(true);
+      await api.cancelReservation(reservationData.id);
       alert('Reservation cancelled successfully.');
       onNavigate('home');
+    } catch (e) {
+      alert(e.message || 'Failed to cancel reservation');
+    } finally {
+      setCancelling(false);
     }
   };
 
@@ -38,7 +51,7 @@ export default function ReservationConfirmedPage({ onNavigate, reservationData }
             Reservation Confirmed
           </h2>
 
-          <div className="bg-green-50 border-2 border-green-200 rounded-xl p-6 mb-6">
+          <div className="bg-violet-50 border-2 border-violet-200 rounded-xl p-6 mb-6">
             <p className="text-xl font-bold text-gray-900 mb-2">
               Success 🎉
             </p>
@@ -68,10 +81,6 @@ export default function ReservationConfirmedPage({ onNavigate, reservationData }
             </p>
           </div>
 
-          <div className="flex space-x-4 text-[#57068C]">
-            <button className="text-sm hover:underline">Add to calendar</button>
-            <button className="text-sm hover:underline">Directions</button>
-          </div>
         </div>
 
         {/* Action Buttons */}
@@ -84,9 +93,10 @@ export default function ReservationConfirmedPage({ onNavigate, reservationData }
 
         <button
           onClick={handleCancelReservation}
+          disabled={cancelling}
           className="w-full py-4 bg-gray-200 text-gray-700 font-semibold text-lg rounded-xl hover:bg-gray-300 transition-colors"
         >
-          Cancel Reservation
+          {cancelling ? 'Cancelling…' : 'Cancel Reservation'}
         </button>
 
         {/* Bottom Spacing */}
